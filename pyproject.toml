[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "B", "DJ"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"ia/services/math_service.py" = ["N802"]

[tool.mypy]
python_version = "3.12"
plugins = ["mypy_django_plugin.main"]

[tool.django-stubs]
django_settings_module = "core.settings"

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "core.settings"
testpaths = [
  "usuarios",
  "clientes",
  "catalogo",
  "compras",
  "pedidos",
  "logistica",
  "auditoria",
  "reportes",
  "inicio",
  "licensing",
  "ia"
]
python_files = ["tests.py", "test_*.py", "*_tests.py"]
norecursedirs = [".venv", "scripts", "htmlcov", "staticfiles"]
addopts = "--cov=usuarios --cov=clientes --cov=catalogo --cov=compras --cov=pedidos --cov=logistica --cov=auditoria --cov=reportes --cov=inicio --cov=licensing --cov=ia --cov-report=html --cov-report=term"
