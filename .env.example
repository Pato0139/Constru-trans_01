# --- Seguridad ---
SECRET_KEY=tu-clave-secreta-de-django
DEBUG=False
ALLOWED_HOSTS=midominio.com,127.0.0.1
CSRF_TRUSTED_ORIGINS=https://midominio.com

# --- Correo (SMTP) ---
# Usa 'django.core.mail.backends.console.EmailBackend' para desarrollo
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=tu-correo@gmail.com
EMAIL_HOST_PASSWORD=tu-contraseña-de-aplicacion-gmail

# --- Base de Datos (PostgreSQL para producción) ---
# Por defecto el proyecto usa SQLite si no se configuran estas variables
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=nombre_db
# DB_USER=usuario_db
# DB_PASSWORD=clave_db
# DB_HOST=localhost
# DB_PORT=5432
